{% extends 'base.html.twig' %}

{% block title %}Chanson{% endblock %}

{% block body %}
    <h1>Chanson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chanson.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chanson.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chanson_index') }}">back to list</a>

    <a href="{{ path('app_chanson_edit', {'id': chanson.id}) }}">edit</a>

    {{ include('chanson/_delete_form.html.twig') }}
{% endblock %}
