{% extends 'base.html.twig' %}

{% block body %}
        <table class="table table-hover">
            <thead>
                <tr>
                <th scope="col">ID Chnateur</th>
                <th scope="col">name</th>>
                <th scope="col">disques</th>
                </tr>
            </thead>
            <tbody>
                <td>{{disque.nameDisque}} </td>
                <td>{{disque.id}}</td>
            </tbody>
{% endblock %}